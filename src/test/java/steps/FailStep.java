package steps;

import cucumber.annotation.en.Given;
import cucumber.annotation.en.Then;
import cucumber.annotation.en.When;
import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.chrome.ChromeDriver;

public class FailStep {


    /*private WebDriver driver;

    @Given("^the user wants to access to the ecommerce$")
    public void theClientIsRegisteredAtThePlatform1() throws InterruptedException {
        System.setProperty("webdriver.chrome.driver", "C:\\git-repos\\automation-practice-pageobject\\driver\\chromedriver.exe");
        driver = new ChromeDriver();
        driver.get("http://automationpractice.com/");
        Thread.sleep(1000);
    }

    @When("^the user fill the login mandatory field$")
    public void theUserFillTheLoginForm1() throws InterruptedException {
        // Click sigIn Form
        driver.findElement(By.className("login")).click();
        Thread.sleep(2000);

        // Fill the login form
        driver.findElement(By.id("email")).sendKeys("nexoqatest@gmail.com");
        driver.findElement(By.id("passwd")).sendKeys("password123");
        driver.findElement(By.cssSelector("#SubmitLogin")).click();
        Thread.sleep(2000);
    }

    @Then("^the user access to the ecommerce$")
    public void theAppShowAErrorMsg() throws InterruptedException {
        driver.get("http://automationpractice.com/");
    }
*/

}
